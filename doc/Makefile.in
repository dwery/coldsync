# Makefile.in
# $Id: Makefile.in,v 1.4 1999-08-23 08:38:38 arensb Exp $

PACKAGE =	@PACKAGE@
VERSION =	@VERSION@

subdir =	doc
srcdir =	@srcdir@
top_srcdir =	@top_srcdir@
curdir =	${top_srcdir}/${subdir}
prefix =	@prefix@
MANDIR =	@mandir@
MAN8DIR =	${MANDIR}/man8

DISTDIR =	${top_srcdir}/${PACKAGE}-${VERSION}

INFOFILES =	libpalm.info \
		pdb.info
DVIFILES =	libpalm.dvi \
		pdb.dvi
PSFILES =	libpalm.ps \
		pdb.ps

EXTRA_DISTFILES = \
		${INFOFILES} \
		${PSFILES}

DISTFILES =	Makefile.in \
		libpalm.texi \
		pdb.texi \
		version.texi.in \
		texinfo.tex \
		coldsync.8 \
		${EXTRA_DISTFILES}

CLEAN =		${INFOFILES} ${DVIFILES} ${PSFILES} \
		*.aux *.cp *.cps *.fn *.ky *.log *.pg *.toc *.tp *.ty *.vr \
		*.bak *~ errs errs.*
DISTCLEAN =	Makefile version.texi .depend
SPOTLESS =

.SUFFIXES:	.dvi .info .ps .texi

.texi.info:
	makeinfo $<

.texi.dvi:
	texi2dvi $<

.dvi.ps:
	dvips -f $< >$@

all:	${INFOFILES}
all-doc:	info dvi ps
info:	${INFOFILES}
dvi:	${DVIFILES}
ps:	${PSFILES}

depend:		# XXX

install::	coldsync.8
	${INSTALL} ${INSTALLARGS} coldsync.8 ${MAN8DIR}

distdir:	${DISTFILES}
	if test ! -d ${DISTDIR}/${subdir}; then \
		mkdir ${DISTDIR}/${subdir}; \
	fi
	cp -p ${DISTFILES} ${DISTDIR}/${subdir}

clean:
	-rm -f ${CLEAN}
distclean:
	-rm -f ${CLEAN} ${DISTCLEAN}
spotless:
	-rm -f ${CLEAN} ${DISTCLEAN} ${SPOTLESS}
